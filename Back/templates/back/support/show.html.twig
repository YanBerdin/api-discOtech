{% extends 'base.html.twig' %}

{% block title %}Support{% endblock %}

{% block body %}

        <h1>Support</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ support.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ support.name }}</td>
                </tr>
            </tbody>
        </table>

<div class="d-flex justify-content-between" style="margin-bottom: 5rem; margin-top: 5rem">
        <a class="btn btn-secondary me-2" href="{{ path('app_back_support_index') }}"> <i class="bi bi-arrow-left"> </i>Retour a la liste</a>
        <div class="d-flex justify-content-end">
            <a class="btn btn-warning me-4" href="{{ path('app_back_support_edit', {'id': support.id}) }}">Modifier</a>
            {{ include('back/support/_delete_form.html.twig') }}
        </div>
</div>   
{% endblock %}
