<form method="post" action="{{ path('app_back_support_delete', {'id': support.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ support.id) }}">
    <button class="btn btn-danger me-2">Supprimer</button>
</form>
