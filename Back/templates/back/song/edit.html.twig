{% extends 'base.html.twig' %}

{% block title %}Edition Musiques{% endblock %}

{% block body %}
    <h1>Modifier une musique</h1>

    {{ include('back/song/_form.html.twig', {'button_label': 'Sauvgarder'}) }}
    <div class="d-flex justify-content-start">
        <a class="btn btn-secondary me-2"href="{{ path('app_back_song_index') }}"><i class="bi bi-arrow-left"></i> Retour à la liste</a>
        {{ include('back/song/_delete_form.html.twig') }}
    </div>
{% endblock %}
