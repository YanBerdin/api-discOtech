{% extends 'base.html.twig' %}

{% block title %}Artistes{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom/Pseudo</th>
                <td>{{ artist.fullname }}</td>
            </tr>
        </tbody>
    </table>

<div class="d-flex justify-content-between" style="margin-bottom: 5rem; margin-top: 5rem">
    <a class="btn btn-secondary me-2" href="{{ path('app_back_artist_index') }}"> <i class="bi bi-arrow-left"> </i>Retour a la liste</a>
    <div class="d-flex justify-content-end">
        <a class="btn btn-warning me-4" href="{{ path('app_back_artist_edit', {'id': artist.id}) }}">Modifier</a>
        {{ include('back/artist/_delete_form.html.twig') }}
    </div>
</div>}
{% endblock %}
