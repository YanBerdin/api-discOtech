{% extends 'base.html.twig' %}

{% block title %}Artistes{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom/Pseudo</th>
                <td>{{ artist.fullname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_artist_index') }}">Retour à la liste</a>

    <a href="{{ path('app_back_artist_edit', {'id': artist.id}) }}">Modifier</a>

    {{ include('back/artist/_delete_form.html.twig') }}
{% endblock %}
