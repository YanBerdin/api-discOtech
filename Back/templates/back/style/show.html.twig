{% extends 'base.html.twig' %}

{% block title %}Style{% endblock %}

{% block body %}
    <h1>Style</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ style.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ style.image }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-flex justify-content-between" style="margin-bottom: 5rem; margin-top: 5rem">
    <a class="btn btn-secondary me-2" href="{{ path('app_back_style_index') }}"> <i class="bi bi-arrow-left"> </i>Retour a la liste</a>
        <div class="d-flex justify-content-end">
            <a class="btn btn-warning me-4" href="{{ path('app_back_style_edit', {'id': style.id}) }}">Modifier</a>
             {{ include('back/style/_delete_form.html.twig') }}
        </div>
</div>
{% endblock %}
